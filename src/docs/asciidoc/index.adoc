= REST API 문서
:doctype: book
:icons: font
:source-highlighter: highlightjs
:toc: left
:toclevels: 3
:sectlinks:

[[overview]]
= 개요

[[overview-http-verbs]]
== HTTP 동사

본 REST API에서 사용하는 HTTP 동사(verbs)는 가능한 한 표준 HTTP와 REST 규약을 따릅니다.

|===
| 동사 | 용례

| `GET`
| 리소스를 가져올 때 사용

| `POST`
| 새 리소스를 만들 때 사용

| `PUT`
| 기존 리소스를 수정할 때 사용

| `PATCH`
| 기존 리소스의 일부를 수정할 때 사용

| `DELETE`
| 기존 리소스를 삭제할 때 사용
|===

[[overview-http-status-codes]]
== HTTP 상태 코드

본 REST API에서 사용하는 HTTP 상태 코드는 가능한 한 표준 HTTP와 REST 규약을 따릅니다.

|===
| 상태 코드 | 용례

| `200 OK`
| 요청을 성공적으로 처리함

| `201 Created`
| 새 리소스를 성공적으로 생성함

| `204 No Content`
| 기존 리소스를 성공적으로 수정함

| `400 Bad Request`
| 잘못된 요청을 보낸 경우

| `401 Unauthorized`
| 인증이 필요한 리소스에 인증 없이 접근한 경우

| `404 Not Found`
| 요청한 리소스가 없음

| `500 Internal Server Error`
| 서버 내부 오류
|===

[[resources-auth]]
= 인증 (Auth)

[[resources-auth-signup]]
== 일반 회원가입

`POST` 요청을 사용해서 새로운 사용자를 생성합니다.

operation::auth-signup[snippets='http-request,http-response']

[[resources-auth-login]]
== 일반 로그인

이메일과 비밀번호를 사용하여 로그인하고, 액세스 토큰 및 리프레시 토큰을 발급받습니다.

operation::auth-login[snippets='http-request,http-response']

[[resources-auth-exchange-token]]
== OAuth2 토큰 교환

OAuth2 로그인(Google 등) 성공 후 리다이렉트된 임시 코드를 사용하여 액세스 토큰을 교환합니다.

operation::auth-exchange-token[snippets='http-request,http-response']
